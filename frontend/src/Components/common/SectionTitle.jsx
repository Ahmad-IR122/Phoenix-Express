function SectionTitle({ title, subtitle }) {
  return (
    <div className="phoenix-section-title text-end">
      <h2>{title}</h2>
      <p>{subtitle}</p>
    </div>
  );
}

export default SectionTitle;